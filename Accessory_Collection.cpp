#include <bits/stdc++.h>
using namespace std;

int main(){
    int t; cin>>t;
    while(t--){
        long long l,a,n,d; cin>>l>>a>>n>>d;
        if()
        long long ans=0;
        for(int i=0; i<l; i+=d){
            ans+=(d*a);
            a--;
        }
        cout<<ans<<endl;
    }
}