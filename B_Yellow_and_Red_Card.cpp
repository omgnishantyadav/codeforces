#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,q;
    vector<pair<int,int>> v(q);
    for(int i=0; i<q; i++){
        cin>>v[i].first;
        cin>>v[i].second;
    }
    for(int i=0
}